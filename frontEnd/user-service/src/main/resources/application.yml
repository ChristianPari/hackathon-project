server:
  port: 3002

spring:
  datasource:
    url:
    username: admin
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  application:
    name: USER-SERVICE
